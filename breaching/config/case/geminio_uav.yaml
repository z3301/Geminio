name: geminio_uav

defaults:
  - data: UAVScenes
  - impl: default
  - server: honest-but-curious
  - user: local_gradient
  - _self_
model: ResNet18
no_dropout: True

user:
  provide_labels: True
  num_data_points: 8

# Server and user:
num_queries: 1
